# Copie para .env e preencha com os dados do seu projeto Supabase
# (Project Settings -> API, em supabase.com). A anon key é segura pra
# expor no navegador — o acesso real é controlado pelas policies de RLS
# (ver supabase/schema.sql).

PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
